feat: Snout MRP — extractProvider, PhantomCluster, HITL gate, CVE check

Implements full Snout MRP Ingestor vision from files.zip prototype:

- extractProvider(): dual-path parse (regex fast + LLM deep), merges
  results, runs CVE cross-check against 13K+ CVE nodes, HITL gate
  blocks Neo4j write if confidence < 70 and creates Linear issue
- generatePhantomClusters(): MRP engine groups PhantomProvider nodes
  into PhantomCluster nodes by strategy (eu_safe_reasoning,
  cost_optimized_code, local_only_privacy, open_source_eu) using
  score = 0.5×avg_conf + 0.3×min(count/5,1) + 0.2×avg_uptime
- getProviderRegistry(): query all PhantomProvider nodes from Neo4j
- New routes: POST /providers, GET /providers, POST /clusters/generate
- New Neo4j labels: PhantomProvider, PhantomCluster (all needsEmbedding)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>