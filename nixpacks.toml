# WidgeTDC Orchestrator — nixpacks build config
# dist/ is pre-built locally (contracts bundled via esbuild)
# Railway only needs to install runtime deps and start

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "node dist/index.js"
